---
title: "Informedb Analysis"
author: "Liam Whitenack"
date: "r Sys.Date()"
output: html_document
runtime: shiny
---

```{r setup, include=FALSE}
suppressPackageStartupMessages(library(tidyverse))
suppressPackageStartupMessages(library(infer))
suppressPackageStartupMessages(library(modelr))
suppressPackageStartupMessages(library(broom))
suppressPackageStartupMessages(library(rlang))
suppressPackageStartupMessages(library(plotly))
suppressPackageStartupMessages(library(shiny))
suppressPackageStartupMessages(library(shinythemes))
suppressPackageStartupMessages(library(dplyr))
suppressPackageStartupMessages(library(readr))
Jira <- read_csv("Informedb Jira Data.csv")
Jira_reduced <- read_csv("Jira_reduced.csv")
```

This R Markdown document is made interactive using Shiny. Unlike the more traditional workflow of creating static reports, you can now create documents that allow your readers to change the assumptions underlying your analysis and see the results immediately. 

To learn more, see [Interactive Documents](http://rmarkdown.rstudio.com/authoring_shiny.html).

## Inputs and Outputs
```{r standard_code_chunk}
ui <- fluidPage(
  sliderInput(inputId = "start_date_resolved", label = "Choose a start date", value = (1451624400), min = (1388552400), max = (1577854800)),
  plotOutput("histogram")
)

server <- function(input, output) {
  output$histogram <- renderPlot({
    title <- "Resolved Issues"
    hist(as.numeric(Jira_reduced$date_resolved), main = title)
  })
}

shinyApp(ui = ui, server = server)
```

